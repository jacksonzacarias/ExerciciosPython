
#si viagem for maior que 200KM  vai cobrar 0,45 por km
#caso for menor que 200km vamos cobrar 0,50

# distancia = int(input('Digite a distancia da viagem: '))
# def Calculo():
#     if distancia > 200:
#         soma = distancia * 0.45     
#     else: 
#         soma = distancia * 0.50 
#     return soma
# print(f'Valor a pagar R${Calculo()} pelos os {distancia}Kms rodados')
# while True:     
#     para = str(input('Deseja continuar? [S/N]')).upper().strip()[0]
#     if para in 'N':
#         break
#     else: para in 'S'
#     distancia = int(input('Digite a distancia da viagem: '))
#     print(f'Valor a pagar R${Calculo()} pelos os {distancia}Kms rodados')

while True:
    distancia = int(input('Digite a distancia da viagem: '))   
    def Calculo():
        if distancia > 200:
            soma = distancia * 0.45     
        else: 
            soma = distancia * 0.50 
        return soma
    print(f'Valor a pagar R${Calculo()} pelos os {distancia}Kms rodados')
    para = str(input('Deseja continuar? [S/N]')).upper().strip()[0]
    if para in 'N':
        break
    
    

# print(f'Valor a pagar R${Calculo()} pelos os {distancia}Kms rodados')
# while True:     
#     para = str(input('Deseja continuar? [S/N]')).upper().strip()[0]
#     if para in 'N':
#         break
#     else: para in 'S'
#     distancia = int(input('Digite a distancia da viagem: '))
#     print(f'Valor a pagar R${Calculo()} pelos os {distancia}Kms rodados')
    
    
        
        
        




        
        
        
# if distancia > 200:
#     soma = distancia * 0.45
#     print(f'Valor a pagar R${soma} pelos os {distancia}Kms rodados')
# else: 
#     soma = distancia * 0.50 
#     print(f'Valor a pagar R${soma} pelos os {distancia}Kms rodados')


    
    
    
    

    
     


